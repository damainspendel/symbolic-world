# Raw copyrighted corpus — stays local, never published.
# Only derived triples + § citations leave this machine.
data/
*.epub

.DS_Store

# Xcode / SwiftPM build artifacts (project regenerated from project.yml via xcodegen)
app/build/
app/RedThread.xcodeproj/
DerivedData/
*.xcuserstate

# Web app build artifacts (data synced from seed.json on build)
web/node_modules/
web/dist/
web/src/graph.json
web/src/pages.json
web/public/graph.json
web/public/pages.json

# runtime artifacts
web/serve.log
*.log
__pycache__/
pipeline/work/
app/
